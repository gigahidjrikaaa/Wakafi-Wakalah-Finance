# .env.example - Environment Variable Template
# --- COMMIT THIS FILE ---

# Server Configuration
PORT=3001

# Database Configuration
MONGO_URI=mongodb://localhost:27017/wakafi_dev # Or your MongoDB Atlas connection string format

# Security (Generate your own strong secrets)
JWT_SECRET= # A long, random, cryptographically secure string for signing JWTs
SESSION_SECRET= # A long, random string for session management (if needed)

# Blockchain Configuration
PHAROS_RPC_URL= # The JSON-RPC endpoint for the Pharos Network (Testnet/Mainnet)
# WAKALAH_POOL_CONTRACT_ADDRESS= # Deployed address of the main WakalahPool contract